// putting export allows multple per file (call with {name}) but export default exports just one
// type details what properties object has and data types
export type Team = {
  id: string;
  teamName: string;
  school: string;
};